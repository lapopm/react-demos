const ProfileCard = () => {
  const styles = {
    backgroundColor: "lightgray",
    padding: "15px",
    borderRadius: "8px",
  };
  return (
    <div style={styles}>
      <h2>Profile Card</h2>
      <p>This is a profile card component.</p>
    </div>
  );
};

export default ProfileCard;
