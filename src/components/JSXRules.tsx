

const JSXRules = () => {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>JSX must return a single parent element</p>
        <p>JSX elements must be properly closed</p>
        <p>JSX attributes are written useing camelCase (e.g., ClassName instead of class).</p>
    </div>
  )
}

export default JSXRules